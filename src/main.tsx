import React from 'react'
import ReactDOM from 'react-dom/client'

import { MonGPT } from './MonGPT';
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MonGPT />
  </React.StrictMode>,
)
